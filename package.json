{
  "name": "as-per-my-last-email",
  "version": "0.1.0",
  "description": "As Per My Last Email — a narrative choice-based office-sabotage game (game jam: theme \"sabotage\").",
  "type": "module",
  "scripts": {
    "placeholders": "node scripts/generate-placeholders.mjs",
    "voices": "node scripts/generate-voice-manifest.mjs",
    "validate": "node scripts/validate-story.mjs",
    "build": "npm run placeholders && npm run voices && npm run validate",
    "build:site": "npm run build && node scripts/build-site.mjs",
    "serve": "node scripts/serve.mjs",
    "start": "npm run placeholders && npm run serve",
    "deploy": "npm run build:site && wrangler pages deploy dist"
  },
  "engines": {
    "node": ">=18"
  },
  "author": "",
  "license": "MIT"
}
